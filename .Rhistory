setwd("~/Desktop/coursera/RepData/RepData_PeerAssessment1")
activitydata
print activitydata
activitydata
print activityData
activity = read.csv("activity.csv", stringsAsFactors = FALSE)
head(activity)
plot(histogram)
plot(histogram)
median_steps <- quantile(x = daily_activity$steps, probs = 0.5)  # Median, 50%Q
daily_activity <- aggregate(formula = steps~date, data = activity, FUN = sum, na.rm=TRUE)
# Calculate summary statistics
mean_steps <- round(mean(daily_activity$steps), 2)  # Mean
median_steps <- quantile(x = daily_activity$steps, probs = 0.5)  # Median, 50%Q
#1.Make a histogram of total number of steps taken each day
histogram <-  qplot(x=date, y=steps,
data=subset(activity, complete.cases(activity)),
data=subset(activity, complete.cases(activity)),
data=subset(activity, complete.cases(activity)),
data=subset(activity, complete.cases(activity)),
data=subset(activity, complete.cases(activity)),
data=subset(activity, complete.cases(activity)),
stat='summary', fun.y=sum, geom='bar') +
labs(title='Figure 1: Number of steps taken daily\n',
y='Total steps per day', x='Date')
plot(histogram)
#2. Calculate and report the mean and median toal number of steps taken each day
```
